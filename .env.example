# OpenAI API key used by every agent in the pipeline.
# Model names are NOT configured here on purpose - they live in src/config.py
# so the graders can read them from source (see README section 9).
OPENAI_API_KEY=sk-...
